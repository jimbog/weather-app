template(name="layout")
    +search
    with weather
        .ui.items
            .item
                .image
                    img(src="{{pic}}")
                .content
                    .name
                        | {{city}}
                    .description
                            div The current temperature is {{temperature}}{{tempUnit}}

template(name="search")
    .ui.action.input
        input(id="city" type="text" placeholder="chicago")
        .ui.button Go
